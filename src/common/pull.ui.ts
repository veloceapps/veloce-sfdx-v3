import {Connection} from '@salesforce/core';

export async function pullUI(sourcepath: string, conn: Connection, dumpAll: boolean, pmlsToDump: Set<string>, pmsToDump: Set<string>): Promise<string[]> {
  return []
}
